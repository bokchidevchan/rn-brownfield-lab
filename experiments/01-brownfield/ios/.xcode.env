# Xcode 빌드 페이즈에서 쓸 node 경로.
# Xcode 는 로그인 셸을 안 거쳐서 nvm/asdf 로 깐 node 를 못 찾습니다.
# 브라운필드에서 "내 터미널에서는 되는데 Xcode 빌드만 실패"의 단골 원인입니다.
#
# 경로를 직접 박아야 하면 .xcode.env.local 을 만들어서 거기에 씁니다.
# 그 파일은 gitignore 돼 있어서 각자 환경이 저장소로 새지 않습니다.
export NODE_BINARY=$(command -v node)
